# Task_Buddy
A lightweight task manager application featuring priority-based task organization, status tracking (pending/completed), and dynamic filtering. Uses local Storage to maintain data across sessions.
